using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class HelicopterPosition : MonoBehaviour
{
    public static HelicopterPosition pos;
    
    private void Awake()
    {
        pos = this;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
